global:
  imagePullSecrets:
  - name: axway-demo-registry

  domainName: "axway-apim-on-eks.de"
  dockerRepository: "ghcr.io/cwiechmann/axway-api-management-automated"
  imageTag: "77-20211130-1-eed8beb"

##################################################################################################################################
#   A D M I N - N O D E M A N A G E R
##################################################################################################################################
anm:
   enabled: true
   ingress:
      enabled: true
      annotations: 
        # https://kubernetes-sigs.github.io/aws-load-balancer-controller/v2.1/guide/ingress/annotations
        kubernetes.io/ingress.class: alb
        alb.ingress.kubernetes.io/scheme: internet-facing
        alb.ingress.kubernetes.io/target-type: ip
        alb.ingress.kubernetes.io/group.name: axway-apim
        alb.ingress.kubernetes.io/listen-ports: '[{"HTTPS": 443}]'
        alb.ingress.kubernetes.io/backend-protocol: "HTTPS"
        # 302 is also accepted, as Kibana is performing a redirect
        alb.ingress.kubernetes.io/success-codes: 200,302

   resources:
      requests:
         cpu: "200m"
         memory: "500Mi"
      limits:
         cpu: "500m"
         memory: "700Mi"

##################################################################################################################################
#   API MANAGER
##################################################################################################################################
apimgr:
   enabled: true
   imageTag: 77-20211130-v1-eed8beb
   ingress:
      enabled: true
      annotations: 
        # https://kubernetes-sigs.github.io/aws-load-balancer-controller/v2.1/guide/ingress/annotations
        kubernetes.io/ingress.class: alb
        alb.ingress.kubernetes.io/scheme: internet-facing
        alb.ingress.kubernetes.io/target-type: ip
        alb.ingress.kubernetes.io/group.name: axway-apim
        alb.ingress.kubernetes.io/listen-ports: '[{"HTTPS": 443}]'
        alb.ingress.kubernetes.io/backend-protocol: "HTTPS"
        # 302 is also accepted, as Kibana is performing a redirect
        alb.ingress.kubernetes.io/success-codes: 200,302

##################################################################################################################################
#   API TRAFFIC
##################################################################################################################################
apitraffic:
   enabled: true
   imageTag: 77-20211130-v1-eed8beb
   ingress:
      enabled: true
      name: traffic
      annotations: 
        # https://kubernetes-sigs.github.io/aws-load-balancer-controller/v2.1/guide/ingress/annotations
        kubernetes.io/ingress.class: alb
        alb.ingress.kubernetes.io/scheme: internet-facing
        alb.ingress.kubernetes.io/target-type: ip
        alb.ingress.kubernetes.io/group.name: axway-apim
        alb.ingress.kubernetes.io/listen-ports: '[{"HTTPS": 443}]'
        alb.ingress.kubernetes.io/backend-protocol: "HTTPS"
        # Healthcheck works by default, but causes with a lot of error 404 requests in Traffic-Monitor
        alb.ingress.kubernetes.io/success-codes: 200,404
        # It's recommended to expose a Healthcheck API
        # alb.ingress.kubernetes.io/healthcheck-path: /healthcheck
        # alb.ingress.kubernetes.io/success-codes: 200

   resources:
      requests:
         cpu: "200m"
         memory: "500Mi"
      limits:
         cpu: "1000m"
         memory: "1Gi"

   extraContainers:
     - name: filebeat
       image: docker.elastic.co/beats/filebeat:7.15.2
       envFrom:
         - configMapRef:
             name: axway-elk-apim4elastic-filebeat-env-config
         - secretRef:
             name: axway-elk-apim4elastic-filebeat-secret
       volumeMounts:
         - name: filebeat-config
           mountPath: /usr/share/filebeat/filebeat.yml
           subPath: filebeat.yml
         - name: certificates
           mountPath: /usr/share/filebeat/config/certificates
         - name: opentraffic
           mountPath: /var/opentraffic
         - name: trace
           mountPath: /var/trace/
         - name: events
           mountPath: /var/events/
         - name: audit
           mountPath: /var/audit/

   extraVolumes:
      - name: filebeat-config
        configMap:
          name: axway-elk-apim4elastic-filebeat-config
      - name: certificates
        secret:
          secretName: axway-elk-apim4elastic-certificates
          

##################################################################################################################################
#   API PORTAL
##################################################################################################################################
apiportal:
   enabled: true
   image: portal
   imageTag: 7.7-20211130
   dockerRepository: docker-registry.demo.axway.com/other-demo
   ingress:
      enabled: true
      name: portal
      annotations: 
        # https://kubernetes-sigs.github.io/aws-load-balancer-controller/v2.1/guide/ingress/annotations
        kubernetes.io/ingress.class: alb
        alb.ingress.kubernetes.io/scheme: internet-facing
        alb.ingress.kubernetes.io/target-type: ip
        alb.ingress.kubernetes.io/group.name: axway-apim
        alb.ingress.kubernetes.io/listen-ports: '[{"HTTPS": 443}]'
        alb.ingress.kubernetes.io/backend-protocol: "HTTPS"

   redis:
      enabled: false
      internal: false

##################################################################################################################################
#   INTERNAL CASSANDRA DATABASE
##################################################################################################################################
cassandra:
   internal: true
   jvm:
      maxHeapSize: 1024m
      newHeapSize: 200m
   resources:
      requests:
         cpu: 1
         memory: 2Gi
      limits:
         cpu: 2
         memory: 2Gi

##################################################################################################################################
#   INTERNAL ANALYTICS/METRICS DATABASE
##################################################################################################################################
mysqlmetrics:
   internal: true

##################################################################################################################################
#   INTERNAL API-Portal DATABASE
##################################################################################################################################
mysqlapiportal:
   internal: true

##################################################################################################################################
#   AMPLIFY AGENTS
##################################################################################################################################

agents:
   central:
      clientId: DOSA_02ea38d92ab64e538214ecb77b17df8f
      organizationId: 230910950274343
      environment: rbi-ho-axway-apigw-v7
      privateKey: |
         -----BEGIN PRIVATE KEY-----
         MIIEvQIBADANBgkqhkiG9w0BAQEFAASCBKcwggSjAgEAAoIBAQCy6I5t5R0qKHWR
         UN7Cgzz2raYhdZzQKWktVRvNnAVaJKrmK981nNIWqB8enooRTDVFEp7XazAZGDVN
         .......
         EEcOAeGzx36oqJx3mUlijZLTZJ/6Mdi9Cv6trapj6e88lB6z3q5wVeKWiLH1f6+d
         UHIriuIHpSr6yz1Foataxpo=
         -----END PRIVATE KEY-----
      publicKey: |
         -----BEGIN PUBLIC KEY-----
         MIIBIjANBgkqhkiG9w0BAQEFAAOCAQ8AMIIBCgKCAQEAsuiObeUdKih1kVDewoM8
         9q2mIXWc0ClpLVUbzZwFWiSq5ivfNZzSFqgfHp6KEUw1RRKe12swGRg1TWr5DBs/
         ......
         jEtwhEjLD44r/Tf50slR7HzjpvoMZabzhxijnAFqhodTPzqLkCFI03IhoOgqZO0t
         ClLsxTTMKk0EqOgjnNY/rlNUplgb7tnfHsEhBbtYWzQFhBQQ+sv8nK/DUljXYP9z
         GwIDAQAB
         -----END PUBLIC KEY-----
   discovery:
      enabled: false
      agentName: "myAgentName"
      apimanager:
         username: apiadmin
         password: highlySecure
         insecureSSL: "true"

##################################################################################################################################
#   PVCs
##################################################################################################################################

pvcs:
   # Events is used by ANM, APIMGR and APITRAFFIC
   events:
      # Not using a persistent volume as Filebeat runs as a sidecar 
      enabled: false
   opentraffic:
      # Not using a persistent volume as Filebeat runs as a sidecar 
      enabled: false
   trace:
      # Not using a persistent volume as Filebeat runs as a sidecar 
      enabled: false
   audit:
      # Not using a persistent volume as Filebeat runs as a sidecar 
      enabled: false